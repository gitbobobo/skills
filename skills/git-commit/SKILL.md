---
name: git-commit
description: 提交变更
---

提交所有变更（纯本地操作）：

1. 先检查 `git status --porcelain` 再决策。
2. 若有变更：按可审计粒度提交，提交信息清晰，且只提交应入库文件。
3. 若发现应忽略文件/临时文件：征求用户意见后决定 1 纳入提交 2 保留并更新 `.gitignore` 3 直接删除。
4. 结束时应尽量保证工作区干净.